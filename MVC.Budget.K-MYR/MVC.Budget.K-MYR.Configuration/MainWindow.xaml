<Window x:Class="MVC.Budget.K_MYR.Configuration.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MVC.Budget.K_MYR.Configuration"
        mc:Ignorable="d"
        Title="The Budgeteer" Height="450" Width="600"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="30">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
               Text="Application Configuration"
               FontSize="24"
               FontWeight="Bold"
               Margin="0,0,0,30"/>

        <StackPanel Grid.Row="1"
                Margin="0,0,0,20">

            <TextBlock Text="Connection String"
                   FontWeight="SemiBold"
                   Margin="0,0,0,5"/>

            <TextBox x:Name="ConnectionStringTextBox"
                 Height="30"/>

        </StackPanel>

        <StackPanel Grid.Row="2">

            <CheckBox x:Name="AutoMigrateCheckBox"
                  Content="Automatically migrate database"
                  Margin="0,0,0,10"/>

            <CheckBox x:Name="SeedDataCheckBox"
                  Content="Seed sample data"/>

        </StackPanel>

        <TextBlock x:Name="StatusTextBlock"
               Grid.Row="3"
               Margin="0,20,0,0"
               TextWrapping="Wrap"/>

        <StackPanel Grid.Row="4"
                Orientation="Horizontal"
                HorizontalAlignment="Right">

            <Button Content="Cancel"
                Width="100"
                Height="35"
                Margin="0,0,10,0"
                Command="{Binding CancelCommand}"/>

            <Button Content="Save"
                Width="100"
                Height="35"
                Command="{Binding SaveCommand}"/>

        </StackPanel>

    </Grid>
</Window>
    
